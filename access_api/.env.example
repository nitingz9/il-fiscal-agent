# =============================================================================
# Illinois Fiscal Data Agent - Environment Configuration
# =============================================================================

# Data source: 'access' (PRIMARY) or 'bigquery' (FALLBACK)
# MS Access is the primary data source - change to 'bigquery' for cloud deployment
DATA_SOURCE=access

# -----------------------------------------------------------------------------
# MS Access Configuration (PRIMARY DATA SOURCE)
# -----------------------------------------------------------------------------
# ODBC Driver - must match your installed driver (check ODBC Data Sources)
ACCESS_DRIVER={Microsoft Access Driver (*.mdb, *.accdb)}

# Path to your Access database file
ACCESS_DB_PATH=C:\FiscalData\data2024.accdb

# -----------------------------------------------------------------------------
# BigQuery Configuration (FALLBACK - for cloud deployments)
# -----------------------------------------------------------------------------
GOOGLE_CLOUD_PROJECT=project-zion-454116
BQ_DATASET=comp_financial_insights_2024
GOOGLE_CLOUD_LOCATION=us-central1

# Gemini API Key (alternative to Vertex AI authentication)
# GOOGLE_API_KEY=your-gemini-api-key

# Use Vertex AI instead of API key
# GOOGLE_GENAI_USE_VERTEXAI=true

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Flask API URL (for ADK agent to call)
FISCAL_API_URL=http://localhost:5000

# Flask API port
API_PORT=5000

# -----------------------------------------------------------------------------
# Model Configuration
# -----------------------------------------------------------------------------
PRIMARY_MODEL=gemini-2.0-flash
